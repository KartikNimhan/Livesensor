What is the purpose of the -p and the -n in conda environemnt?
--> -p full path, -n we don't need to give the full path


 conda create -p venv python=3.8 in the given command we can also write the -y so that later on it won't ask you the permission for procedding the furhter process

git clone https://github.com/KartikNimhan/Livesensor.git

All packages are availale in pypi site

__init__.py
setup.py